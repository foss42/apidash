/// Timeout and other stress test related constants
class StressTestConstants {
  static const Duration defaultTimeout = Duration(seconds: 30);
  static const Duration isolateCommunicationTimeout = Duration(seconds: 10);
}
