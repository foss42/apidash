name: apidash_mcp_server
description: MCP server for API Dash agentic testing
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  http: ^1.2.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0