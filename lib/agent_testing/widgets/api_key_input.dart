import 'package:flutter/material.dart';

/// Deprecated placeholder kept for compatibility with older imports.
/// Agent Testing now uses the configured default AI model from Settings.
class ApiKeyInput extends StatelessWidget {
  const ApiKeyInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}